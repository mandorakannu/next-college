import "./globals.css";
import type { Metadata } from "next";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Core Campus | Kannu Mandora | Full Stack Developer",
  description:
    "The Core Campus is a Fully Functional College Management System based Next.js Project and the owner of this project is Kannu Mandora.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
